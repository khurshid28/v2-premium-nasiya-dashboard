/*eslint-disable*/
import React from "react";

// Footer for auth pages has been intentionally removed per user request.
// Return null so nothing is rendered on the sign-in/auth pages.
export default function Footer(): JSX.Element | null {
  return null;
}
